name: Supabase heartbeat

on:
  schedule:
    - cron: "0 9 */3 * *"   # cada 3 días, a las 9:00 AM UTC
  workflow_dispatch:         # te deja correrlo manualmente desde la pestaña Actions

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s -X GET "https://vdrsdlejmorkoeekxhyh.supabase.co/rest/v1/projects?select=id&limit=1" \
            -H "apikey: sb_publishable_qQK1KW4YgBpOXZLzVXZfZg_W7RxQdsA"
